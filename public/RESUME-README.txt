Place your final resume here as:

public/resume.pdf

The portfolio checks for that exact file at build time. Until it exists, the Resume button is shown as disabled so you cannot accidentally deploy a broken link.
